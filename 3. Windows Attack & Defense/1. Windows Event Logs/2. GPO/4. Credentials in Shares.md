## Description

Credentials exposed in network shares are (probably) the most encountered misconfiguration in Active Directory to date.

We often find credentials in network shares within scripts and configuration files (batch, cmd, PowerShell, conf, ini, and config). In contrast, credentials on a user's local machine primarily reside in text files, Excel sheets, or Word documents.
- Batch and command files (`.bat`, `.cmd`)
- PowerShell scripts (`.ps1`)
- Configuration files (`.conf`, `.ini`, `.config`)

In the case of Hidden shares (folders whose name ends with a dollar sign `$`), we cannot find them without knowing its location; however, some tools can deal with problem.

For example, `\\server\admin$` do not appear in standard share listings.

---
## Attack Path

**The first step is identifying what shares exist in a domain.**
- Using PowerView's [Invoke-ShareFinder](https://github.com/darkoperator/Veil-PowerView/blob/master/PowerView/functions/Invoke-ShareFinder.ps1) allows specifying that default shares should be filtered out (such as `c$` and `IPC$`) and also check if the current user account has at least read access to the rest of the shares it finds.

![](../../Image/Pasted%20image%2020250802232041.png)

![](../../Image/Pasted%20image%2020250803103730.png)

**The second step is parsing a collection of files and picking up matching words.**
- Using [SauronEye](https://github.com/vivami/SauronEye), which can support in enterprise environment.
- There are few shares in the playground, we will take a more manual approach (`Living Off the Land`) and use the built-in command `findstr` for this attack.

Display the path of the files with a match that contains `pass` relative to the current location:

![](../../Image/Pasted%20image%2020250803105402.png)

- `/s` forces to search the current directory and all subdirectories
- `/i` ignores case in the search term
- `/m` shows only the filename for a file that matches the term.

Search term is the `NetBIOS` name of the domain:

![](../../Image/Pasted%20image%2020250803110757.png)

Looking for `pw` as the search term:

![](../../Image/Pasted%20image%2020250803110949.png)

---
## Prevention

The best practice to prevent these attacks is to lock down every share in the domain so there are no loose permissions.

Technically, there is no way to prevent what users leave behind them in scripts or other exposed files, so performing regular scans (e.g., weekly) on AD environments to identify any new open shares or credentials exposed in older ones is necessary.

---
## Detection

Understanding and analyzing users' behavior is the best detection technique for abusing discovered credentials in shares. Suppose we know the time and location of users' login via data analysis. In that case, it will be effortless to alert on seemingly suspicious behaviors—for example, the discovered account 'Administrator' in the attack described above. If we were a mature organization that used `Privileged Access Workstation`, we would be alert to privileged users not authenticating from those machines. These would be alerts on event IDs `4624`/`4625` (failed and successful logon) and `4768` (Kerberos TGT requested).

Another detection technique is discovering the `one-to-many` connections, for example, when `Invoke-ShareFinder` scans every domain device to obtain a list of its network shares. It would be abnormal for a workstation to connect to 100s or even 1000s of other devices simultaneously.

---
## Q & A
1) Connect to the target and enumerate the available network shares. What is the password of the Administrator2 user?
Let’s start by importing the Powerview.ps1 script to utilize Invoke-ShareFinder for identifying domain shares.

![](https://faresbltagy.gitbook.io/footprintinglabs/~gitbook/image?url=https%3A%2F%2F2537271824-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252FIswWWP3l0rGuQmG2WUcr%252Fuploads%252F5cQ5Wn8rzyCRkeRngzZa%252FScreenshot.png%3Falt%3Dmedia%26token%3De84d207a-5e7e-4469-9c49-114e39c5b2db&width=768&dpr=4&quality=100&sign=a55c9ee0&sv=2)

Next, let’s search the dev$ share for the Administrator2 password.

```
findstr /m /s /i "Administrator2" *.ps1
```

![](https://faresbltagy.gitbook.io/footprintinglabs/~gitbook/image?url=https%3A%2F%2F2537271824-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252FIswWWP3l0rGuQmG2WUcr%252Fuploads%252F3CUaPAHKYcmpgiMKghQJ%252FScreenshot%281%29.png%3Falt%3Dmedia%26token%3Dba8100af-aedd-4c7d-8a0c-da5a4b287197&width=768&dpr=4&quality=100&sign=10da891a&sv=2)

Answer: Slavi920